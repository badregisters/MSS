# 最小化诊断配置 — 用于判定问题出在 UniFOM 配置内 还是 SR/设备本身
# 除 loglevel 外全部使用 Shadowrocket 默认值：
#   不覆盖 bypass-system / ipv6 / dns-server / hijack-dns / skip-proxy 等任何 [General] 项
#   无任何分流规则，全部流量走代理
[General]
loglevel = info

[Proxy Group]
🔍 全部节点 = select, policy-regex-filter = .

[Rule]
FINAL,🔍 全部节点
